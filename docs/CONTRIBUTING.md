# Contributing

## Development
1. Fork and clone the repo.
2. Run `npm install`.
3. Use `npm run dev` for local development.
4. Ensure `npm run check` passes before opening a PR.

## Guidelines
- Keep changes scoped and well-tested.
- Update docs when behavior changes.
